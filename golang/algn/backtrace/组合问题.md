#### 从1,2,3,4获取组合问题 
C(4,2) 从4个任选2个自由组合
跟位置无关

### 回溯三部曲
#### 1. 递归函数返回值
#### 2. 终止条件
#### 3. 单层递归逻辑
```go
p:=[]string{}
result:=[][]string{}
func backtracing(n,k int,startIndex int){
	if len(path)==k {
	    tmp:=make([]string,len(p))
		copy(tmp,p)
		result = append(result,tmp)
		return 
    }
	for i:=startIndex;i<n;i++{
	    p = append(p,i)
		backtracing(n,k,i+10)
		p = p[:len(p)-1]
    }
}
```